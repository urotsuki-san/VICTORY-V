# VV64-A0 Architecture and FPGA Subset

## Status

`VV64-A0` is the native 64-bit continuation of `VV32-A0`.

The architecture contract is still a draft. The repository now contains a synthesizable FPGA subset and self-checking RTL tests. That subset is intentionally smaller than the eventual Linux-capable machine.

| Area | FPGA subset | Full VV64-A0 contract |
|---|---|---|
| 64-bit integer state and PC | yes | yes |
| inherited VV32 primary opcodes | yes | yes |
| 64-bit capability load/store | yes | yes |
| protected Capability Directory | yes | yes |
| Secret Tags | yes | yes |
| Victory Regions and `VLOCK` | yes | yes |
| basic CSRs, traps, interrupt input | yes | yes |
| tagged context spill/fill | no | required |
| Monitor, Supervisor, User | no | required |
| atomics and fences | no | required |
| sealed calls and returns | no | required |
| `VTRYA` | no | required |
| caches, DDR3, page translation | no | optional/later |

Unsupported extension-page operations trap as illegal instructions.

## Base machine

| Property | `VV64-A0` |
|---|---|
| Instruction width | fixed 32 bits |
| Integer width | 64 bits |
| Registers | 32 architectural registers |
| Program counter | 64 bits, four-byte aligned |
| Byte order | little-endian |
| Architectural address width | 64 bits |
| First FPGA physical width | 17 bits in the bring-up image |
| Privilege | Monitor, Supervisor, User in the full profile |
| Translation | optional and absent from the first image |
| First core | single-issue, in order, non-speculative |
| Nested Victory Regions | prohibited in A0 |
| SMP, FPU, vectors | outside A0 |

An implementation that supports fewer than 64 physical address bits must trap when unsupported high bits are used. It must not silently alias them.

`r0` remains zero and untagged. `rN`, `cN`, and `sN` are aliases for the same register state.

## Encoding continuity

The primary opcode positions from `VV32-A0` are unchanged. The FPGA core implements the inherited base operations directly and uses `XMEM` for doubleword access:

```text
XMEM function 0: CLDD
XMEM function 1: CSTD
```

The rest of the extension pages remain reserved for the architecture work:

| Opcode | Page | Purpose |
|---:|---|---|
| `0x36` | `XALU` | division, remainder, word operations, constant lanes |
| `0x37` | `XMEM` | 64-bit and tagged context memory operations |
| `0x38` | `XATOM` | reservation, compare/exchange, atomic RMW, fences |
| `0x39` | `XCALL` | sealed entry, protected call and return |
| `0x3a` | `XSYS` | privilege, context, interrupt, and domain operations |
| `0x3b` | `XVM` | optional page translation and TLB maintenance |
| `0x3c`–`0x3f` | reserved | left unused in A0 |

Only the two listed `XMEM` functions are assigned by the current RTL. The other sub-opcode layouts are not frozen.

## Tagged register state

Each register has:

```text
value[63:0]
capability tag
Capability Directory index + generation
secret tag
```

The FPGA subset uses a 32-entry directory. Each entry contains 64-bit base and top bounds, permissions, validity, and an 8-bit generation. Reusing an entry changes its generation, so an older register reference fails instead of silently acquiring the replacement authority.

Integer-producing instructions clear the destination capability tag. Copying or moving a capability preserves its directory reference. `CBOUNDS` and `CPERM` allocate a narrowed entry. `CINC` changes only the cursor and checks the existing bounds.

The small directory and finite generation field are prototype choices, not the final context ABI.

## Stored capabilities

The full architecture adds tagged context memory:

```text
CLDC    load a cursor and its authority metadata
CSTC    store a cursor and its authority metadata
```

These operations are not in the current FPGA subset. Ordinary integer stores carry no capability tag. Task switching and Linux therefore wait for tagged context storage.

## Privilege and domains

### Monitor

Monitor mode owns reset, initial roots, the Capability Directory, domain creation, and optional translation roots. It delegates authority and then executes `VLOCK`.

### Supervisor

Supervisor mode is the Linux kernel level. It manages processes, interrupts, devices, and optional page tables within authority delegated by Monitor.

### User

User mode runs inside a domain with capabilities for code, data, stack, and kernel entry.

The current FPGA subset runs one unprivileged bring-up environment and does not yet distinguish these modes.

## Physical and paged modes

Physical mode is mandatory and is the only mode implemented by the first RTL image.

The optional `V39` profile adds 39-bit virtual addresses, 4 KiB base pages, three page-table levels, larger leaves, TLBs, and page faults. Its page-table format is not frozen.

The fixed rule remains:

```text
effective permission = capability ∩ domain ∩ page ∩ privilege
```

Translation may deny an access. It may not add authority.

## Sealed control flow

The full architecture uses executable capabilities, sealed call gates, protected return tokens, `CALLC`, and `RETC`. The first FPGA subset leaves `XCALL` unimplemented.

## Secret flow

Secret data may be processed, but it cannot select a branch, indirect target, address, or public store. Declassification still requires explicit `D` authority.

The current core scrubs secret registers when a Victory Region aborts. It does not claim to stop power, electromagnetic, or every timing side channel.

## Victory Regions

The inherited form remains:

```asm
vtry   failed, stores, budget
...
vic
```

Stores are forwarded to the executing core but remain outside memory until `VIC`. A fault or failed `VCHK` discards them, records `VERROR`, scrubs secret registers, and reaches the failure edge.

The full VV64 contract also defines `VTRYA`, which aborts on an enabled interrupt. `VTRYA` is not in the first FPGA subset.

MMIO and atomics remain prohibited inside A0 regions.

## First FPGA configuration

```text
one VV32-A0 core
one VV64-A0 bring-up core
separate on-chip RAM
shared UART and status page
fixed ROM
no DDR3, caches, MMU, FPU, vectors, or SMP
```

The shared image proves that both native cores can coexist behind one board layer. It does not yet provide shared memory, coherency, scheduling, or a Linux boot path.

See [`FPGA_138K_BRINGUP.md`](FPGA_138K_BRINGUP.md).

## Open items

- complete extension-page encodings;
- tagged context layout and memory tags;
- privilege and trap-frame layout;
- atomic memory model;
- sealed call and return-token storage;
- interrupt-abort regions;
- compiler and linker ABI;
- DDR3 bridge and cache/tag coherence;
- flat Linux port;
- `V39` page-table bits and invalidation rules;
- debugger access to tagged state;
- Victory Region behavior across debug halt and reset.
